name: collection_providers
description: A group of pre-built providers the wrap the basic dart collections - list, set, map
version: 0.0.1+1
homepage: https://flutter.institute/

environment:
  sdk: ">=2.7.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^4.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  pedantic: ^1.9.0                  # Static analysis rules

flutter:
