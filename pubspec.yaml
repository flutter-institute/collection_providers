name: collection_providers
description: A group of pre-built providers the wrap the basic dart collections
  - list, set, map
version: 0.2.2
homepage: https://flutter.institute/
repository: https://github.com/flutter-institute/collection_providers
issue_tracker: https://github.com/flutter-institute/collection_providers/issues

environment:
  sdk: ">=2.12.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.3
